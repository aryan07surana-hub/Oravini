Replit AI Prompt

Build and modify my SaaS client portal with the following product structure and UX improvements. Focus on making the UI clean, premium, and intuitive.

1. Tracking Section Layout

When the user clicks “Tracking” in the sidebar:

Do NOT show anything in the sidebar.

Instead show three large cards on the main screen:

Sales Metrics

Ad Metrics

Content Metrics

These should appear as 3 large clickable boxes in the center of the screen.

When a user clicks one of them, they should be taken into that specific metrics dashboard.

2. Content Metrics Section

When the user clicks Content Metrics, show three options:

Instagram

YouTube

Calendar

These should appear as large cards on the screen.

3. Instagram Content Metrics

Inside the Instagram section:

Posts should be organized month by month.

Instead of showing a giant table, show monthly boxes like:

January

February

March

April

Each month should be displayed as a square card.

Example:
User clicks March →

Then open the March dashboard showing:

All reels/posts logged that month

Daily logs (what was posted each day)

Metrics for each reel

When a new month starts, the system should automatically create a new month section.

When a user logs a post, it should automatically go to the correct month section.

4. Log Post Editing (Fix Current Issue)

Currently logged posts cannot be edited.

Fix this.

Users must be able to:

Edit their logged post anytime

Update metrics later (2 weeks, 4 weeks updates)

Change views, likes, comments, saves

When edits happen, the system must automatically recalculate all metrics.

5. Engagement Rate Calculation

For every reel calculate Engagement Rate automatically.

Formula:

(likes + comments + saves) / views * 100

When users input:

Views

Likes

Comments

Saves

The system automatically calculates:

Engagement Rate (%)

Display this per reel.

6. Conversion Rate / Engagement Tracking Updates

Each reel should have options to update metrics:

Initial metrics

After 2 weeks

After 4 weeks

Each time the metrics are updated:

Recalculate engagement automatically.

7. Visual Analytics (Make It Premium)

Create visual dashboards for Instagram analytics.

Include:

Pie charts

Engagement distribution

Performance graphs

Monthly engagement trends

Make it visually premium SaaS style.

8. AI Report Generator

Add a button:

“Generate Report”

When clicked:

Show a 5 second loading animation

Display an AI style loading icon

Then generate a visual analytics report

Report should include:

Engagement insights

Top performing reels

Average engagement

Growth trends

9. Calendar Section

Inside Content Metrics → Calendar

Create a calendar where users can see:

Content posted on each day

Instagram posts

YouTube posts

Calendar should visually mark posting days.

10. Admin Dashboard Improvements

Admin should also have access to:

Calendar View

Admin can see:

Scheduled calls

Client bookings

Content schedules

11. Call Booking Integration

Whenever a client books a call:

It should automatically appear in the Admin Dashboard Calendar.

Admin should see:

Client name

Time

Date

Meeting status

12. Homepage Section (Coming Soon)

On the homepage add a section:

“Course Modules”

Inside this section:

Add a placeholder card

Label it Coming Soon

Later this will redirect to course content.

13. Admin Dashboard Extras

Add a small Admin Utility Panel showing:

Time Zones

Display live clocks for:

New York

London

Dubai

Daily Admin Quote

Add a widget that displays:

A new motivational quote every day.

14. UI/UX Design Requirements

The entire dashboard should look:

Premium SaaS

Minimal

Clean

Modern

Use:

Cards

Soft shadows

Graphs

Smooth transitions

Loading animations

Avoid big tables whenever possible.

Focus on dashboard style visuals.